<Root>
<% @datasets.each do |d| %>
    <intermedia>
        <email><%= d.institutional_mail  %></email>
        <subcategoria><%= get_label_for_id(id: d.category).strip %></subcategoria>
        <institucion><%= get_label_for_id(id: d.institution).strip  %></institucion>
    </intermedia>
<% end %>
</Root>
