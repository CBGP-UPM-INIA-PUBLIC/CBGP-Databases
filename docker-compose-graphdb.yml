services:
  graphdb:
    image: ontotext/graphdb:10.8.0
    restart: always
    hostname: graphdb
    ports:
      - 7200:7200
    volumes:
      - cbgp-graphdb:/opt/graphdb/home

volumes:
  cbgp-graphdb:
    external: true

